---
phase: 01-desktop-app-registration-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/container-platform.routes.tsx
  - src/pages/container-platform/ContainerPlatformSidebar.tsx
  - src/pages/container-platform/OverviewPage.tsx
  - src/pages/container-platform/ClustersPage.tsx
  - src/pages/container-platform/NodesPage.tsx
  - src/pages/container-platform/WorkloadsPage.tsx
  - src/pages/DesktopPage.tsx
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, TDS-01]

must_haves:
  truths:
    - "A 'Container Platform' icon appears on the desktop grid"
    - 'Clicking the icon opens Container Platform in its own desktop window'
    - 'The window opens on /container-platform/overview'
    - 'A persistent left sidebar lists Overview / Clusters / Nodes / Workloads'
    - 'Clicking each sidebar item navigates to that section and highlights it as active'
    - 'All shell chrome renders via @/design-system components (no raw HTML layout containers)'
  artifacts:
    - path: 'src/routes/container-platform.routes.tsx'
      provides: 'Lazy route fragment for the 4 container-platform sections'
      contains: 'containerPlatformRoutes'
    - path: 'src/pages/container-platform/ContainerPlatformSidebar.tsx'
      provides: 'Persistent 4-item sidebar with active-route highlighting'
      contains: 'ContainerPlatformSidebar'
    - path: 'src/pages/container-platform/OverviewPage.tsx'
      provides: 'Overview placeholder page (PageShell + PageHeader)'
      contains: 'PageShell'
    - path: 'src/pages/container-platform/ClustersPage.tsx'
      provides: 'Clusters placeholder page'
      contains: 'PageShell'
    - path: 'src/pages/container-platform/NodesPage.tsx'
      provides: 'Nodes placeholder page'
      contains: 'PageShell'
    - path: 'src/pages/container-platform/WorkloadsPage.tsx'
      provides: 'Workloads placeholder page'
      contains: 'PageShell'
    - path: 'src/pages/DesktopPage.tsx'
      provides: '5-place app registration (AppId, AppRoutes case, appConfigs, DESKTOP_ICONS_META, route import)'
      contains: 'container-platform'
  key_links:
    - from: 'src/pages/DesktopPage.tsx (AppRoutes switch)'
      to: 'containerPlatformRoutes'
      via: 'import from @/routes/container-platform.routes + <Routes>{containerPlatformRoutes}</Routes>'
      pattern: 'containerPlatformRoutes'
    - from: 'src/routes/container-platform.routes.tsx'
      to: 'src/pages/container-platform/*Page.tsx'
      via: 'lazy() imports mapped to <Route> elements'
      pattern: "lazy\\(\\(\\) => import\\('@/pages/container-platform/"
    - from: 'ContainerPlatformSidebar MenuItem hrefs'
      to: '/container-platform/{overview,clusters,nodes,workloads}'
      via: 'MenuItem href + isActive(location.pathname)'
      pattern: '/container-platform/overview'
---

<objective>
Deliver the Container Platform Walking Skeleton: register a new `container-platform`
desktop app and build its windowed shell so an operator can open the app from the desktop
and navigate an empty 4-section sidebar (Overview / Clusters / Nodes / Workloads).

Purpose: Prove the app registers, opens in its own desktop window, and routes between
sections before any real data or list UI lands (Phases 2-8). This is the thinnest
end-to-end slice for the Container Platform product.

Output: A new route fragment, a 4-item sidebar, 4 TDS placeholder pages, and the 5-place
DesktopPage registration wiring them into the desktop shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-desktop-app-registration-shell/01-CONTEXT.md
@prerequisite.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. Use these directly. -->

LOCKED app identity (from 01-CONTEXT.md):

- AppId literal: 'container-platform'
- Display name: 'Container Platform'
- Desktop icon: reuse the already-imported `imgContainer` binding in DesktopPage.tsx
  (from `@/assets/appIcon/container.webp`) — do NOT add a new import or image asset.
- initialPath: '/container-platform/overview'

DesktopPage.tsx existing registration shapes to model:

- AppId union (~line 1149): a string-literal union; add `| 'container-platform'`.
- Route import block (~lines 53-64): `import { containerRoutes } from '@/routes/container.routes';`
  — add an analogous `import { containerPlatformRoutes } from '@/routes/container-platform.routes';`.
- AppRoutes switch (~line 1230): each case returns a `<Routes>{...}</Routes>`. The `container`
  case (~1261-1274) wraps ContainerModeContext + AppCatalogModeContext providers — DO NOT
  copy those providers; model the minimal `case 'iam': return <Routes>{iamRoutes}</Routes>;`
  shape instead.
- appConfigs record (~line 2252): `Record<AppId, { name: string; icon: string; initialPath: string }>`.
  Existing entry: `container: { name: 'Container', icon: imgContainer, initialPath: '/container' }`.
- DESKTOP_ICONS_META array (~line 151): entries shaped `{ id, icon, label }`, e.g.
  `{ id: 'aegis-container', icon: imgContainer, label: 'Aegis Container' }`.

Route fragment shape (src/routes/container.routes.tsx):

- `const X = lazy(() => import('@/pages/...'));` then
  `export const containerRoutes = (<> <Route path="..." element={<X />} /> ... </>);`

Sidebar contract (src/components/ContainerSidebar.tsx):

- Uses `MenuSection` + `MenuItem` from '@/design-system' with props
  `icon`, `label`, `href`, `active`. Active state computed via
  `const location = useLocation(); const isActive = (href) => location.pathname === href || location.pathname.startsWith(href + '/');`
- Navigation: MenuItem `href` drives react-router navigation.

Page layout contract (src/pages/ContainerNodesPage.tsx return, ~line 400):

- `<PageShell sidebar={<...Sidebar/>} topBar={<TopBar ... breadcrumb={<Breadcrumb items={[{label:'...'}]}/> />}>`
  wrapping `<VStack gap={3}><PageHeader title="..." /> ...body... </VStack>`.
- Import surface: `PageShell, PageHeader, TopBar, Breadcrumb, VStack` from '@/design-system'.

TDS text note: there is NO exported `Typography` component. Render the placeholder body
text the way the rest of the codebase does — a `<span>` with TDS token classes
(e.g. `className="text-body-md text-[var(--color-text-muted)]"`) inside a TDS `VStack`.
This keeps TDS-01 compliance (VStack is the layout primitive; span is text, not layout).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Container Platform shell (routes + sidebar + 4 placeholder pages)</name>
  <read_first>
    - src/routes/container.routes.tsx (route fragment pattern to copy)
    - src/components/ContainerSidebar.tsx (MenuSection/MenuItem nav + isActive pattern)
    - src/pages/ContainerNodesPage.tsx (PageShell → TopBar/Breadcrumb → VStack → PageHeader layout)
    - prerequisite.md (TDS layout rules, forbidden raw HTML)
    - .planning/phases/01-desktop-app-registration-shell/01-CONTEXT.md (LOCKED decisions)
  </read_first>
  <files>
    src/routes/container-platform.routes.tsx,
    src/pages/container-platform/ContainerPlatformSidebar.tsx,
    src/pages/container-platform/OverviewPage.tsx,
    src/pages/container-platform/ClustersPage.tsx,
    src/pages/container-platform/NodesPage.tsx,
    src/pages/container-platform/WorkloadsPage.tsx
  </files>
  <action>
    Create the new directory `src/pages/container-platform/` and the route fragment
    `src/routes/container-platform.routes.tsx`.

    Route fragment: copy the lazy-loaded `<Route>` structure of container.routes.tsx but
    export a fragment named `containerPlatformRoutes` with exactly 4 routes:
    `/container-platform/overview` → OverviewPage, `/container-platform/clusters` → ClustersPage,
    `/container-platform/nodes` → NodesPage, `/container-platform/workloads` → WorkloadsPage.
    Add a catch-all `<Route path="/container-platform/*" element={<Navigate to="/container-platform/overview" replace />} />`.
    Use `lazy(() => import('@/pages/container-platform/OverviewPage'))` style imports.

    ContainerPlatformSidebar.tsx: model the MenuSection/MenuItem structure from
    ContainerSidebar.tsx but STRIP OUT the ContainerMode/cluster/drawer/icon-sidebar
    complexity. Render a single persistent left sidebar (one MenuSection titled
    "Container Platform" is acceptable) with exactly 4 MenuItems — Overview, Clusters, Nodes,
    Workloads — whose `href` values are the 4 routes above. Compute `active` with a local
    `isActive(href)` using `useLocation()` (`location.pathname === href || location.pathname.startsWith(href + '/')`).
    Pick any 4 reasonable tabler/lucide icons already used in the codebase (e.g.
    IconLayoutDashboard, FolderCog, IconTopologyStar, IconRocket). Do NOT depend on
    useContainerMode or ContainerModeContext.

    4 placeholder pages (OverviewPage, ClustersPage, NodesPage, WorkloadsPage): each renders
    `<PageShell sidebar={<ContainerPlatformSidebar/>} topBar={<TopBar showNavigation onBack/onForward via useNavigate breadcrumb={<Breadcrumb items={[{ label: '<Section>' }]} />} />}>`
    wrapping `<VStack gap={3}><PageHeader title="<Section>" /><span className="text-body-md text-[var(--color-text-muted)]">Coming in a later phase</span></VStack>`.
    Do NOT include TabBar, ShellPanel, ContainerTopBarActions, tables, or list data — those
    are deferred. Import ONLY from '@/design-system' (PageShell, PageHeader, TopBar, Breadcrumb,
    VStack) and react-router's useNavigate. No raw `<table>`/`<input>`/`<select>` and no raw
    layout `<div>` where a TDS primitive exists (per TDS-01 and prerequisite.md).

  </action>
  <verify>
    <automated>test -f src/routes/container-platform.routes.tsx && grep -q "containerPlatformRoutes" src/routes/container-platform.routes.tsx && for p in OverviewPage ClustersPage NodesPage WorkloadsPage ContainerPlatformSidebar; do test -f "src/pages/container-platform/$p.tsx" || { echo "MISSING $p"; exit 1; }; done && grep -Rq "PageShell" src/pages/container-platform/OverviewPage.tsx && grep -Rq "/container-platform/overview" src/pages/container-platform/ContainerPlatformSidebar.tsx && ! grep -Rn "@thaki/shared" src/pages/container-platform src/routes/container-platform.routes.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/container-platform.routes.tsx exists and exports `containerPlatformRoutes` with the 4 section routes + overview catch-all.
    - The 4 page files and ContainerPlatformSidebar.tsx exist under src/pages/container-platform/.
    - Each page file references `PageShell` and renders a `PageHeader` with its section title.
    - ContainerPlatformSidebar lists all 4 hrefs (`/container-platform/overview|clusters|nodes|workloads`) and computes an active state from the current location.
    - No file under the new dir or the new route file imports from `@thaki/shared` (TDS-01: only `@/design-system`).
    - No raw `<table>`, `<input>`, or `<select>` in the new files.
  </acceptance_criteria>
  <done>All shell files exist; routes export the 4-section fragment; sidebar links the 4 routes with active highlighting; pages render TDS PageShell/PageHeader placeholders; no @thaki/shared imports.</done>
</task>

<task type="auto">
  <name>Task 2: Register container-platform in DesktopPage (5 places + route import)</name>
  <read_first>
    - src/pages/DesktopPage.tsx (AppId union ~1149, route import block ~53-64, AppRoutes switch ~1230, container case ~1261-1274, iam case ~1290 for the minimal shape, appConfigs ~2252, DESKTOP_ICONS_META ~151, icon imports ~95-117)
    - src/routes/container-platform.routes.tsx (the fragment created in Task 1 — confirm export name)
    - .planning/phases/01-desktop-app-registration-shell/01-CONTEXT.md (5-place registration spec)
  </read_first>
  <files>src/pages/DesktopPage.tsx</files>
  <action>
    Make exactly these edits in src/pages/DesktopPage.tsx, reusing the existing `imgContainer`
    binding (do NOT add a new icon import — the container.webp asset is already imported ~line 98):

    1. Route import: add `import { containerPlatformRoutes } from '@/routes/container-platform.routes';`
       alongside the existing `containerRoutes` import (~lines 53-64).
    2. AppId union (~line 1149): add `| 'container-platform'` to the union.
    3. AppRoutes switch (~line 1230): add a new `case 'container-platform':` that returns
       `<Routes>{containerPlatformRoutes}</Routes>` — MINIMAL, modeled on the `iam` case.
       Do NOT wrap it in ContainerModeContext or AppCatalogModeContext providers.
    4. appConfigs record (~line 2252): add
       `'container-platform': { name: 'Container Platform', icon: imgContainer, initialPath: '/container-platform/overview' },`.
    5. DESKTOP_ICONS_META array (~line 151): add
       `{ id: 'container-platform', icon: imgContainer, label: 'Container Platform' },`.

    Do not modify dockAppOrder or any other app's registration. Because appConfigs is typed
    `Record<AppId, ...>`, the union edit and the appConfigs edit must both land or the type
    check fails — make both.

  </action>
  <verify>
    <automated>grep -q "container-platform.routes" src/pages/DesktopPage.tsx && grep -q "'container-platform'" src/pages/DesktopPage.tsx && grep -q "case 'container-platform'" src/pages/DesktopPage.tsx && grep -q "containerPlatformRoutes" src/pages/DesktopPage.tsx && grep -q "/container-platform/overview" src/pages/DesktopPage.tsx && grep -q "label: 'Container Platform'" src/pages/DesktopPage.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - DesktopPage.tsx imports `containerPlatformRoutes` from `@/routes/container-platform.routes`.
    - The AppId union contains `'container-platform'`.
    - AppRoutes has `case 'container-platform':` returning `<Routes>{containerPlatformRoutes}</Routes>` with NO ContainerMode/AppCatalog providers.
    - appConfigs has a `'container-platform'` entry with name `Container Platform`, icon `imgContainer`, initialPath `/container-platform/overview`.
    - DESKTOP_ICONS_META has `{ id: 'container-platform', icon: imgContainer, label: 'Container Platform' }`.
    - No new image import was added (imgContainer reused).
  </acceptance_criteria>
  <done>All 5 registration points plus the route import are present; the app is wired into the desktop grid, window opener, and router switch using the reused container icon.</done>
</task>

</tasks>

<verification>
- `pnpm lint` passes for the new files and the DesktopPage edit (no unused imports, no type errors surfaced by eslint).
- Optional integrity gate: `npx tsc --noEmit` reports no new errors introduced by the AppId union / appConfigs edits.
- Grep gates in each task's `<automated>` block pass.
- No `@thaki/shared` import anywhere under src/pages/container-platform/ or in container-platform.routes.tsx.
</verification>

<success_criteria>

- The 5-place registration + route import are present in DesktopPage.tsx.
- The route fragment, sidebar, and 4 placeholder pages exist and are TDS-only.
- The app is reachable from the desktop grid and opens on `/container-platform/overview`
  (final human-verified E2E lives in plan 01-02).
  </success_criteria>

<output>
Create `.planning/phases/01-desktop-app-registration-shell/01-01-SUMMARY.md` when done.
</output>
