---
phase: 01-desktop-app-registration-shell
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-03, TDS-01]

must_haves:
  truths:
    - 'Operator can open Container Platform from the desktop and navigate all 4 sections with no console errors'
  artifacts: []
  key_links:
    - from: "desktop icon 'Container Platform'"
      to: 'windowed shell on /container-platform/overview'
      via: 'click → IsolatedRouter MemoryRouter initialPath'
      pattern: 'manual E2E'
---

<objective>
Human-verify the Walking Skeleton end-to-end: the Container Platform app opens from the
desktop, renders in its own window, and its sidebar navigates all 4 sections with no
console errors.

Purpose: This is the acceptance gate for Phase 1. There is no automated E2E runner in this
mockup, so a human confirms the full slice works after the autonomous implementation (01-01).

Output: Verified running skeleton (or a list of defects to feed a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-desktop-app-registration-shell/01-CONTEXT.md
@.planning/phases/01-desktop-app-registration-shell/01-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the Container Platform walking skeleton end-to-end</name>
  <read_first>
    - .planning/phases/01-desktop-app-registration-shell/01-01-PLAN.md (what was built)
    - .planning/phases/01-desktop-app-registration-shell/01-CONTEXT.md (verify/run steps)
  </read_first>
  <what-built>
    A new `container-platform` desktop app: desktop icon, windowed shell, a persistent
    4-item sidebar (Overview / Clusters / Nodes / Workloads), and 4 TDS placeholder pages
    that open on `/container-platform/overview`.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` from the project root (rspack, port 5180).
    2. Open http://localhost:5180/desktop.
    3. Confirm a "Container Platform" icon appears on the desktop grid (reuses the container icon).
    4. Click the icon → a draggable desktop window opens on the Overview section
       (`/container-platform/overview`), showing a "Container Platform" sidebar and a
       PageHeader titled "Overview".
    5. Click each sidebar item (Overview, Clusters, Nodes, Workloads) → the content area
       switches to that section, the PageHeader title changes, and the clicked item is
       highlighted as active.
    6. Open the browser devtools console and confirm NO errors on any of the 4 sections.
  </how-to-verify>
  <acceptance_criteria>
    - PLAT-01: the Container Platform icon is visible on the desktop grid.
    - PLAT-02: clicking it opens the app in its own desktop window on /container-platform/overview.
    - PLAT-03: the persistent sidebar navigates all 4 sections and highlights the active one.
    - TDS-01: shell chrome renders via @/design-system (no broken/raw layout, no visual regressions).
    - No console errors on any section.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all steps pass, or describe the defects (which will seed a `--gaps` closure plan).</resume-signal>
</task>

</tasks>

<verification>
- All 6 how-to-verify steps pass with no console errors.
</verification>

<success_criteria>

- Operator confirms the walking skeleton opens and navigates end-to-end (PLAT-01/02/03, TDS-01).
  </success_criteria>

<output>
Create `.planning/phases/01-desktop-app-registration-shell/01-02-SUMMARY.md` when done.
</output>
