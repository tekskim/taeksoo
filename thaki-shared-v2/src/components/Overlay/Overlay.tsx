import OverlayContainer from './Overlay.Container';
import OverlayTemplate, { type OverlayProps } from './Overlay.Template';

const Overlay = {
  Template: OverlayTemplate,
  Container: OverlayContainer,
};

export { Overlay, OverlayProps };
